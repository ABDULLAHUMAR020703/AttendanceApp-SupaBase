# Local npm start only (api-gateway does not load dotenv — defaults are localhost).
# For Coolify / docker compose, use the root .env.example instead.
#
# Optional overrides if you export these in your shell:
PORT=3000
HOST=0.0.0.0
AUTH_SERVICE_URL=http://localhost:3001
REPORTING_SERVICE_URL=http://localhost:3002
