# Local npm start only (api-gateway does not load dotenv — defaults are localhost).
# For Coolify / docker compose, use the root .env.example instead.
#
# Optional overrides if you export these in your shell:
PORT=3000
HOST=0.0.0.0
AUTH_SERVICE_URL=http://localhost:3001
REPORTING_SERVICE_URL=http://localhost:3002

# Auth enforcement (export in your shell for local `npm start`; set via root
# .env / Coolify for compose). Without SUPABASE_URL + SUPABASE_ANON_KEY the
# gateway runs in legacy pass-through mode and does NOT verify caller JWTs.
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
INTERNAL_API_SECRET=
